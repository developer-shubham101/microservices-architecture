package com.example.sbbloginfo.messaging;

/**
 * BlogCreatedEventListener removed: listener logic intentionally disabled.
 *
 * The original listener consumed messages from the broker and saved blog info.
 * To keep the code history and avoid removing config, this file remains as a
 * stub. If you want to restore the listener, reintroduce @Component and the
 * @RabbitListener method implementation.
 */
public class BlogCreatedEventListener {
}
